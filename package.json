{
  "name": "server-log-console",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build:extension": "npm --prefix apps/extension run build"
  }
}